{
  "name": "valkey-common",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^4.0.16"
  }
}

services:
  app:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile.app
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      KEY_VALUE_SIZE_LIMIT_BYTES: 2048
